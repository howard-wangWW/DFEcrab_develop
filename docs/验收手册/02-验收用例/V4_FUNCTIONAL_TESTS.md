# DFEcrab v4.0 功能验收用例

> **文档版本**: 1.0  
> **创建日期**: 2026-04-03  
> **验收阶段**: v4.0 发布前验收  
> **验收标准**: 所有用例通过方可发布  

---

## 1. 验收概述

### 1.1 验收目标

验证 DFEcrab v4.0 的 7 个核心模块满足产品需求规格，具备发布条件。

### 1.2 验收范围

| 模块 | 验收用例数 | 优先级 |
|-----|-----------|-------|
| 权限系统 | 8 | P0 |
| 斜杠命令系统 | 6 | P0 |
| 工具系统 | 10 | P0 |
| 会话持久化 | 5 | P1 |
| 上下文引擎 | 4 | P1 |
| 工作区配置 | 5 | P1 |
| Agent 循环 | 6 | P0 |
| **总计** | **44** | - |

### 1.3 验收通过标准

| 指标 | 要求 |
|-----|------|
| 用例通过率 | 100% |
| P0 缺陷 | 0 |
| P1 缺陷 | 0 |
| 核心功能完整性 | 100% |
| 用户体验 | 符合设计规范 |

---

## 2. 权限系统验收用例

### AC-PERM-001: 四级权限模型

**验收目标**: 验证四级权限模型符合产品设计

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 启动 DFEcrab 默认模式 | 权限级别为 READ_ONLY | 通过 |
| 2 | 使用 `--allow-write` 启动 | 权限级别为 WRITE | 通过 |
| 3 | 使用 `--allow-shell` 启动 | 权限级别为 SHELL | 通过 |
| 4 | 使用 `--unsafe` 启动 | 权限级别为 UNSAFE | 通过 |
| 5 | 验证权限逐级递增 | READ_ONLY < WRITE < SHELL < UNSAFE | 通过 |

---

### AC-PERM-002: 默认安全策略

**验收目标**: 验证默认安全策略有效

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 默认模式下尝试写入文件 | 操作被拒绝，提示需要 `--allow-write` | 通过 |
| 2 | 默认模式下尝试执行 Shell 命令 | 操作被拒绝，提示需要 `--allow-shell` | 通过 |
| 3 | 默认模式下尝试破坏性命令 | 操作被拒绝，提示需要 `--unsafe` | 通过 |
| 4 | 验证错误提示清晰易懂 | 提示包含具体解决方案 | 通过 |

---

### AC-PERM-003: 破坏性命令防护

**验收目标**: 验证破坏性命令检测覆盖全面

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 尝试 `rm -rf` 命令 | 被拦截（SHELL 模式下） | 通过 |
| 2 | 尝试 `dd` 磁盘写入命令 | 被拦截 | 通过 |
| 3 | 尝试 `shutdown/reboot` | 被拦截 | 通过 |
| 4 | 尝试 `mkfs` 格式化命令 | 被拦截 | 通过 |
| 5 | 尝试 `git reset --hard` | 被拦截 | 通过 |
| 6 | 尝试 `git clean -fd` | 被拦截 | 通过 |
| 7 | 尝试 `chmod -R 777` | 被拦截 | 通过 |
| 8 | 在 UNSAFE 模式下执行上述命令 | 命令可执行，但有警告提示 | 通过 |

---

### AC-PERM-004: 权限状态可视化

**验收目标**: 验证权限状态可清晰查看

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/permissions` 命令 | 显示当前权限配置 | 通过 |
| 2 | 验证输出包含各权限状态 | 使用 ✅/❌ 标识 | 通过 |
| 3 | 验证输出包含启用方法 | 提示如何启用各权限 | 通过 |
| 4 | 执行 `/tools` 命令 | 显示各工具的权限状态 | 通过 |

---

### AC-PERM-005: 权限配置持久化

**验收目标**: 验证权限配置可持久化

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 在配置文件中设置权限 | 配置保存成功 | 通过 |
| 2 | 重启 DFEcrab | 权限配置保持 | 通过 |
| 3 | 验证命令行参数覆盖配置 | 参数优先级高于配置 | 通过 |

---

### AC-PERM-006: 工具级权限控制

**验收目标**: 验证工具级别权限控制精确

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | READ_ONLY 模式下调用 write_file | 被拒绝 | 通过 |
| 2 | READ_ONLY 模式下调用 edit_file | 被拒绝 | 通过 |
| 3 | READ_ONLY 模式下调用 bash | 被拒绝 | 通过 |
| 4 | READ_ONLY 模式下调用 read_file | 成功执行 | 通过 |
| 5 | READ_ONLY 模式下调用 list_dir | 成功执行 | 通过 |
| 6 | READ_ONLY 模式下调用 glob_search | 成功执行 | 通过 |
| 7 | READ_ONLY 模式下调用 grep_search | 成功执行 | 通过 |

---

### AC-PERM-007: 权限错误处理

**验收目标**: 验证权限错误处理友好

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 触发写入权限错误 | 错误信息包含 "Enable --allow-write" | 通过 |
| 2 | 触发 Shell 权限错误 | 错误信息包含 "Enable --allow-shell" | 通过 |
| 3 | 触发破坏性命令拦截 | 错误信息包含 "需要 --unsafe" | 通过 |
| 4 | 验证错误不导致系统崩溃 | 系统继续正常运行 | 通过 |

---

### AC-PERM-008: 权限审计日志

**验收目标**: 验证权限操作可审计

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行权限变更操作 | 日志记录变更 | 通过 |
| 2 | 执行被拒绝的操作 | 日志记录拒绝事件 | 通过 |
| 3 | 执行破坏性命令（UNSAFE 模式） | 日志记录警告 | 通过 |
| 4 | 检查日志格式 | 包含时间戳、操作、结果 | 通过 |

---

## 3. 斜杠命令系统验收用例

### AC-CMD-001: 命令解析准确性

**验收目标**: 验证斜杠命令解析准确

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 输入 `/help` | 显示命令列表 | 通过 |
| 2 | 输入 `/help extra` | 正常处理，忽略额外参数 | 通过 |
| 3 | 输入 `/model qwen-max` | 解析命令和参数 | 通过 |
| 4 | 输入非命令文本 | 传递给模型处理 | 通过 |
| 5 | 输入 `/` 后无内容 | 提示命令格式 | 通过 |
| 6 | 输入 `/unknown_command` | 提示未知命令 | 通过 |

---

### AC-CMD-002: 核心命令功能

**验收目标**: 验证 10 个核心命令功能正常

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/help` 或 `/commands` | 显示所有可用命令 | 通过 |
| 2 | 执行 `/context` 或 `/usage` | 显示上下文用量估算 | 通过 |
| 3 | 执行 `/context-raw` 或 `/env` | 显示原始上下文快照 | 通过 |
| 4 | 执行 `/prompt` 或 `/system-prompt` | 显示系统提示 | 通过 |
| 5 | 执行 `/permissions` | 显示权限配置 | 通过 |
| 6 | 执行 `/model` | 显示模型配置 | 通过 |
| 7 | 执行 `/tools` | 显示工具列表 | 通过 |
| 8 | 执行 `/memory` | 显示内存信息 | 通过 |
| 9 | 执行 `/status` 或 `/session` | 显示会话状态 | 通过 |
| 10 | 执行 `/clear` | 清理临时状态 | 通过 |

---

### AC-CMD-003: 命令别名支持

**验收目标**: 验证命令别名正确工作

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/commands` | 等同于 `/help` | 通过 |
| 2 | 执行 `/usage` | 等同于 `/context` | 通过 |
| 3 | 执行 `/env` | 等同于 `/context-raw` | 通过 |
| 4 | 执行 `/system-prompt` | 等同于 `/prompt` | 通过 |
| 5 | 执行 `/session` | 等同于 `/status` | 通过 |
| 6 | 验证别名输出一致 | 主命令和别名输出相同 | 通过 |

---

### AC-CMD-004: 本地处理不依赖模型

**验收目标**: 验证斜杠命令本地处理

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 断开 LLM 服务 | 服务不可用 | 通过 |
| 2 | 执行 `/help` | 正常显示命令列表 | 通过 |
| 3 | 执行 `/status` | 正常显示状态 | 通过 |
| 4 | 执行 `/permissions` | 正常显示权限配置 | 通过 |
| 5 | 验证无模型调用 | 无网络请求发出 | 通过 |

---

### AC-CMD-005: 命令帮助完整性

**验收目标**: 验证命令帮助信息完整

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/help` | 显示所有 10 个命令 | 通过 |
| 2 | 验证每个命令有描述 | 描述清晰说明功能 | 通过 |
| 3 | 验证别名在帮助中显示 | 别名正确列出 | 通过 |
| 4 | 验证帮助格式统一 | 使用 Markdown 格式 | 通过 |

---

### AC-CMD-006: 命令扩展性

**验收目标**: 验证命令系统可扩展

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 注册新命令 | 注册成功 | 通过 |
| 2 | 执行新注册的命令 | 正常执行 | 通过 |
| 3 | 新命令出现在 `/help` 中 | 帮助列表更新 | 通过 |
| 4 | 移除已注册命令 | 移除成功 | 通过 |

---

## 4. 工具系统验收用例

### AC-TOOL-001: 核心工具可用性

**验收目标**: 验证 7 个核心工具可用

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 调用 `list_dir` 列出目录 | 返回目录内容 | 通过 |
| 2 | 调用 `read_file` 读取文件 | 返回文件内容 | 通过 |
| 3 | 调用 `write_file` 写入文件 | 文件创建成功 | 通过 |
| 4 | 调用 `edit_file` 编辑文件 | 文件内容更新 | 通过 |
| 5 | 调用 `glob_search` 搜索文件 | 返回匹配文件 | 通过 |
| 6 | 调用 `grep_search` 搜索内容 | 返回匹配行 | 通过 |
| 7 | 调用 `bash` 执行命令 | 返回命令输出 | 通过 |

---

### AC-TOOL-002: 工具参数验证

**验收目标**: 验证工具参数验证正确

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | `read_file` 缺少 path 参数 | 返回参数错误 | 通过 |
| 2 | `write_file` 缺少 content 参数 | 返回参数错误 | 通过 |
| 3 | `edit_file` 提供不存在的 old_text | 返回 "old_text not found" | 通过 |
| 4 | `bash` 缺少 command 参数 | 返回参数错误 | 通过 |
| 5 | `list_dir` 提供不存在的 path | 返回 "Path not found" | 通过 |

---

### AC-TOOL-003: 路径安全防护

**验收目标**: 验证路径安全防护有效

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 尝试读取 `../secret_file` | 被拒绝，提示逃逸工作区 | 通过 |
| 2 | 尝试读取 `/etc/passwd` | 被拒绝（工作区外） | 通过 |
| 3 | 尝试写入 `../../malicious` | 被拒绝 | 通过 |
| 4 | 在工作区内正常读写 | 正常执行 | 通过 |
| 5 | 验证自动创建父目录 | `write_file` 自动创建目录 | 通过 |

---

### AC-TOOL-004: 工具权限集成

**验收目标**: 验证工具与权限系统集成

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | READ_ONLY 模式调用写工具 | 被拒绝 | 通过 |
| 2 | READ_ONLY 模式调用读工具 | 正常执行 | 通过 |
| 3 | WRITE 模式调用写工具 | 正常执行 | 通过 |
| 4 | SHELL 模式调用 bash（安全命令） | 正常执行 | 通过 |
| 5 | SHELL 模式调用 bash（破坏命令） | 被拒绝 | 通过 |
| 6 | UNSAFE 模式调用 bash（破坏命令） | 正常执行，有警告 | 通过 |

---

### AC-TOOL-005: 工具输出格式

**验收目标**: 验证工具输出格式规范

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行成功工具 | 返回 ok=True 的结果 | 通过 |
| 2 | 执行失败工具 | 返回 ok=False 的结果 | 通过 |
| 3 | 验证结果可序列化 | 可转换为 JSON | 通过 |
| 4 | 验证大输出被截断 | 超过限制时截断 | 通过 |
| 5 | 验证截断标记清晰 | 包含 "[truncated]" 标记 | 通过 |

---

### AC-TOOL-006: 流式工具执行

**验收目标**: 验证流式工具执行正常

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 流式执行 bash 命令 | 实时输出进度 | 通过 |
| 2 | 验证 stdout 流式输出 | 逐行输出 | 通过 |
| 3 | 验证 stderr 流式输出 | 错误流式输出 | 通过 |
| 4 | 验证最终结果返回 | 包含退出码 | 通过 |
| 5 | 流式执行非 bash 工具 | 分块输出内容 | 通过 |

---

### AC-TOOL-007: OpenAI 格式兼容

**验收目标**: 验证工具与 OpenAI API 兼容

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 调用 `to_openai_tool()` | 返回 OpenAI 格式 | 通过 |
| 2 | 验证格式符合规范 | 包含 type 和 function | 通过 |
| 3 | 验证可直接用于 API | 格式正确 | 通过 |
| 4 | 验证所有工具可转换 | 7 个工具全部可转换 | 通过 |

---

### AC-TOOL-008: 工具错误处理

**验收目标**: 验证工具错误处理健壮

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行超时命令 | 返回超时错误 | 通过 |
| 2 | 读取大文件 | 正常截断输出 | 通过 |
| 3 | 执行无效命令 | 返回错误信息 | 通过 |
| 4 | 验证错误不崩溃系统 | 系统继续运行 | 通过 |
| 5 | 验证错误信息清晰 | 包含具体原因 | 通过 |

---

### AC-TOOL-009: 工具报告

**验收目标**: 验证工具报告生成正确

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 生成工具报告 | 报告包含所有工具 | 通过 |
| 2 | 验证权限状态显示 | ✅/❌ 标识正确 | 通过 |
| 3 | 验证工具总数显示 | "Total: 7 tools" | 通过 |
| 4 | 验证权限提示显示 | 包含权限提示字符串 | 通过 |
| 5 | 验证报告为 Markdown | 格式正确 | 通过 |

---

### AC-TOOL-010: 工具扩展性

**验收目标**: 验证工具系统可扩展

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 注册新工具 | 注册成功 | 通过 |
| 2 | 执行新工具 | 正常执行 | 通过 |
| 3 | 新工具出现在报告中 | 报告更新 | 通过 |
| 4 | 新工具可配置权限 | 权限控制生效 | 通过 |

---

## 5. 会话持久化验收用例

### AC-SESS-001: 会话保存功能

**验收目标**: 验证会话可正确保存

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 创建会话并添加消息 | 会话包含消息 | 通过 |
| 2 | 调用保存功能 | 保存到持久化存储 | 通过 |
| 3 | 验证保存文件存在 | 文件在预期位置 | 通过 |
| 4 | 验证文件内容完整 | 包含所有消息和元数据 | 通过 |
| 5 | 验证保存性能 | < 500ms | 通过 |

---

### AC-SESS-002: 会话恢复功能

**验收目标**: 验证会话可正确恢复

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 加载已保存的会话 | 加载成功 | 通过 |
| 2 | 验证消息完整性 | 消息数量和内容一致 | 通过 |
| 3 | 验证元数据一致 | ID、时间等一致 | 通过 |
| 4 | 验证会话可继续使用 | 添加新消息正常 | 通过 |
| 5 | 验证加载性能 | < 500ms | 通过 |

---

### AC-SESS-003: 会话更新

**验收目标**: 验证会话更新正确保存

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 加载会话并添加消息 | 消息添加成功 | 通过 |
| 2 | 再次保存会话 | 保存成功 | 通过 |
| 3 | 重新加载验证 | 包含新消息 | 通过 |
| 4 | 验证旧数据不丢失 | 原有消息仍在 | 通过 |

---

### AC-SESS-004: 异常处理

**验收目标**: 验证会话异常处理健壮

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 加载不存在的会话 ID | 返回明确错误 | 通过 |
| 2 | 加载损坏的会话文件 | 优雅处理，不崩溃 | 通过 |
| 3 | 并发保存同一会话 | 无数据损坏 | 通过 |
| 4 | 磁盘空间不足时保存 | 返回明确错误 | 通过 |

---

### AC-SESS-005: 会话管理

**验收目标**: 验证会话管理功能完整

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 列出所有会话 | 列表正确 | 通过 |
| 2 | 删除指定会话 | 删除成功 | 通过 |
| 3 | 验证删除后不可加载 | 返回不存在错误 | 通过 |
| 4 | 会话自动清理（可选） | 过期会话被清理 | 通过 |

---

## 6. 上下文引擎验收用例

### AC-CTX-001: 上下文信息管理

**验收目标**: 验证上下文信息管理正确

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 设置工作区信息 | 信息存储成功 | 通过 |
| 2 | 设置 Agent 信息 | 信息存储成功 | 通过 |
| 3 | 设置会话信息 | 信息存储成功 | 通过 |
| 4 | 获取上下文快照 | 包含所有信息 | 通过 |
| 5 | 更新上下文信息 | 更新生效 | 通过 |

---

### AC-CTX-002: 上下文大小估算

**验收目标**: 验证上下文大小估算合理

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/context` 命令 | 返回估算值 | 通过 |
| 2 | 验证估算值合理 | 与实际用量接近 | 通过 |
| 3 | 添加消息后重新估算 | 估算值增加 | 通过 |
| 4 | 清理消息后重新估算 | 估算值减少 | 通过 |

---

### AC-CTX-003: 上下文快照

**验收目标**: 验证上下文快照功能

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/context-raw` 命令 | 返回原始上下文 | 通过 |
| 2 | 验证包含 Workspace | 路径正确 | 通过 |
| 3 | 验证包含 Agent ID | ID 正确 | 通过 |
| 4 | 验证包含 Session ID | ID 正确 | 通过 |

---

### AC-CTX-004: 上下文清理

**验收目标**: 验证上下文清理功能

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 执行 `/clear` 命令 | 清理临时状态 | 通过 |
| 2 | 验证核心数据保留 | 会话消息不丢失 | 通过 |
| 3 | 验证缓存清理 | 缓存被清空 | 通过 |

---

## 7. 工作区配置验收用例

### AC-WKS-001: 配置加载

**验收目标**: 验证工作区配置正确加载

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 从 `dfecrab.json` 加载配置 | 加载成功 | 通过 |
| 2 | 验证必要字段 | workspace, model, permissions 等 | 通过 |
| 3 | 验证默认值填充 | 缺失字段使用默认值 | 通过 |
| 4 | 验证配置验证 | 无效配置报错 | 通过 |
| 5 | 验证加载性能 | < 100ms | 通过 |

---

### AC-WKS-002: 配置验证

**验收目标**: 验证配置验证规则

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 提供无效配置 | 验证错误 | 通过 |
| 2 | 错误信息清晰 | 指出具体字段 | 通过 |
| 3 | 提供有效配置 | 验证通过 | 通过 |
| 4 | 验证必填字段检查 | 缺少必填字段报错 | 通过 |

---

### AC-WKS-003: 配置覆盖

**验收目标**: 验证配置可被环境变量覆盖

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 设置环境变量 | 环境变量生效 | 通过 |
| 2 | 验证环境变量优先级 | 高于配置文件 | 通过 |
| 3 | 验证命令行参数优先级 | 高于环境变量 | 通过 |

---

### AC-WKS-004: 配置热重载

**验收目标**: 验证配置热重载功能

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 修改配置文件 | 文件保存成功 | 通过 |
| 2 | 触发热重载 | 配置重新加载 | 通过 |
| 3 | 验证新配置生效 | 新配置值生效 | 通过 |
| 4 | 验证重载期间服务不中断 | 服务持续可用 | 通过 |

---

### AC-WKS-005: 配置持久化

**验收目标**: 验证配置修改可持久化

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 修改配置 | 修改成功 | 通过 |
| 2 | 保存配置 | 保存到文件 | 通过 |
| 3 | 重启后验证 | 配置保持 | 通过 |

---

## 8. Agent 循环验收用例

### AC-AGT-001: Agent 循环基本功能

**验收目标**: 验证 Agent 循环正常工作

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 启动 Agent 循环 | 循环初始化成功 | 通过 |
| 2 | 输入普通查询 | 进入模型查询 | 通过 |
| 3 | 模型返回响应 | 响应返回给用户 | 通过 |
| 4 | 验证消息历史维护 | 消息正确记录 | 通过 |
| 5 | 多轮对话 | 上下文正确传递 | 通过 |

---

### AC-AGT-002: 工具调用集成

**验收目标**: 验证 Agent 循环中工具调用正常

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 模型请求调用工具 | 工具被调用 | 通过 |
| 2 | 工具执行成功 | 结果返回给模型 | 通过 |
| 3 | 模型基于结果继续 | 生成后续响应 | 通过 |
| 4 | 工具执行失败 | 错误传递给模型 | 通过 |
| 5 | 多轮工具调用 | 循环正确处理 | 通过 |

---

### AC-AGT-003: 斜杠命令集成

**验收目标**: 验证 Agent 循环中斜杠命令处理

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 输入斜杠命令 | 本地处理，不查询模型 | 通过 |
| 2 | 命令执行成功 | 结果直接返回 | 通过 |
| 3 | 验证消息历史 | 命令交互记录在历史中 | 通过 |
| 4 | 未知命令 | 返回友好提示 | 通过 |

---

### AC-AGT-004: 权限检查集成

**验收目标**: 验证 Agent 循环中权限检查

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 模型请求写入文件（无权限） | 请求被拒绝 | 通过 |
| 2 | 拒绝信息返回给模型 | 模型知晓权限限制 | 通过 |
| 3 | 模型请求 Shell 命令（无权限） | 请求被拒绝 | 通过 |
| 4 | 有权限时正常执行 | 工具正常执行 | 通过 |

---

### AC-AGT-005: 迭代限制

**验收目标**: 验证 Agent 循环迭代限制

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 设置最大迭代次数 | 限制生效 | 通过 |
| 2 | 达到限制 | 循环停止 | 通过 |
| 3 | 返回达到限制提示 | 提示清晰 | 通过 |
| 4 | 验证消息历史完整 | 历史不丢失 | 通过 |

---

### AC-AGT-006: 异常处理

**验收目标**: 验证 Agent 循环异常处理

| 步骤 | 操作 | 预期结果 | 验收标准 |
|-----|------|---------|---------|
| 1 | 模型调用失败 | 异常被捕获 | 通过 |
| 2 | 工具执行异常 | 异常被捕获 | 通过 |
| 3 | 网络异常 | 异常被捕获 | 通过 |
| 4 | 验证循环不崩溃 | 循环继续可用 | 通过 |
| 5 | 验证错误信息传递 | 错误信息返回给用户 | 通过 |

---

## 9. 验收统计

### 9.1 用例分布

| 模块 | 用例数 | P0 | P1 |
|-----|-------|-----|-----|
| 权限系统 | 8 | 8 | 0 |
| 斜杠命令系统 | 6 | 6 | 0 |
| 工具系统 | 10 | 10 | 0 |
| 会话持久化 | 5 | 0 | 5 |
| 上下文引擎 | 4 | 0 | 4 |
| 工作区配置 | 5 | 0 | 5 |
| Agent 循环 | 6 | 6 | 0 |
| **总计** | **44** | **30** | **14** |

### 9.2 验收检查清单

- [ ] 所有 P0 用例通过
- [ ] 所有 P1 用例通过
- [ ] 无 P0/P1 缺陷
- [ ] 性能指标达标
- [ ] 用户体验符合设计
- [ ] 文档完整更新
- [ ] 发布检查清单完成

---

## 10. 验收审批

| 角色 | 姓名 | 日期 | 意见 | 签名 |
|-----|------|------|------|------|
| 验收工程师 | | | | |
| QA Lead | | | | |
| 开发经理 | | | | |
| 产品经理 | | | | |
| 发布经理 | | | | |

---

*文档结束*
