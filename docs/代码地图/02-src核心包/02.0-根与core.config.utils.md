# 02.0 · `src/` 根目录与 core / config / utils

> 覆盖范围：`src/` 根下 3 个文件 + `core/`（3）+ `config/`（4）+ `utils/`（7）= **17 个文件**
>
> 分层：L6 基础设施层。这一组是所有其他模块的底座——路径、配置、日志、事件协议、错误处理。
>
> 上一页：[01-顶层目录总览.md](../01-顶层目录总览.md)　｜　下一页：[02.1-agent与LLM.md](./02.1-agent与LLM.md)

---

## 0. 本组文件一览

| 文件 | 行数 | 类型 | 一句话职责 |
|---|---|---|---|
| `src/__init__.py` | 58 | 工具类 | 定义 `PROJECT_ROOT` 与 6 个 `get_xxx_path()` 路径工具 |
| `src/__main__.py` | 213 | 脚本 | argparse 次入口（gateway / tui / mcp-server） |
| `src/agentscope_compat.py` | 20 | 兼容壳 | 为旧 skill 提供最小 AgentScope 响应对象 |
| `core/__init__.py` | 7 | 常量枚举 | 仅 docstring，声明保留模块 |
| `core/errors.py` | 218 | 核心逻辑 | 统一错误处理体系 |
| `core/event_types.py` | 304 | 常量枚举 | ReAct 事件协议定义与构造 |
| `config/__init__.py` | 15 | 配置 | 导出 `config.py` 的符号 |
| `config/config.py` | 234 | 配置 | **硬编码模型配置**的单例（含明文 API Key） |
| `config/config_loader.py` | 99 | 配置 | 读 `gateway.yaml` 的配置单例 |
| `config/port_loader.py` | 93 | 配置 | 本地端口单一事实源 |
| `utils/__init__.py` | 11 | 工具类 | 导出日志处理器与时间解析 |
| `utils/logger.py` | 7 | 工具类 | `logging.getLogger` 的薄封装 |
| `utils/log_handler.py` | 70 | 工具类 | 单文件滚动 + 按天裁剪的 Handler |
| `utils/logging_setup.py` | 146 | 工具类 | 统一日志初始化 + 启动 Banner |
| `utils/mermaid_validator.py` | 260 | 工具类 | Mermaid 代码块校验与自动修复 |
| `utils/time_parser.py` | 214 | 工具类 | 中文自然语言时间 → 具体日期 |
| `utils/word_reader.py` | 36 | 工具类 | 读取 .docx 文本 |

---

## 1. `src/` 根文件

### 1.1 `src/__init__.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 路径工具 |
| 规模 | 58 行 / 0 类 / 7 个模块级函数 |
| 职责 | 定义项目根与配置目录，提供各资源目录的路径解析函数 |
| 核心符号 | `PROJECT_ROOT`、`CONFIG_DIR`、`get_project_path()`、`get_data_path()`、`get_config_path()`、`get_plugins_path()`、`get_skills_path()`、`get_agents_path()`；`__version__ = "4.0.0"` |
| 被调用方 | 全仓库大量模块 |
| 依赖 | `pathlib` |
| 关键行为 | `PROJECT_ROOT = Path(__file__).resolve().parent.parent`（即 `src/` 的上一级） |
| 备注 | **`__version__ = "4.0.0"`，但 `README.md` 写的是 v4.1、`dfecrab` 是 `4.0.0-grpc`、`src/__main__.py` 是 `v3.0.0`——四个版本号互不一致**；`get_plugins_path()` 已无实际调用方（`plugins/` 未被加载） |

### 1.2 `src/__main__.py`

| 项 | 内容 |
|---|---|
| 类型 | 脚本 · 次入口 |
| 规模 | 213 行 / 0 类 / 5 函数 |
| 职责 | `python -m src <command>` 的入口，提供 gateway / tui / mcp-server 三组命令 |
| 核心符号 | `main()`、`run_gateway()`、`run_mcp_server()`、`run_tui()`、`run_tui_remote()`、`_run_node_tui()` |
| 被调用方 | 命令行（`python -m src ...`） |
| 依赖 | `src.gateway.grpc_server.GatewayV2GRPC`、`src.gateway.legacy`、`src.services.service_registry`、`src.mcp.create_mcp_server`、`src.config.config_loader` |
| 关键行为 | ① `--mode grpc`（默认，含 WebSocket）走 `GatewayV2GRPC`；`--mode plugin`（旧版，无 WS）走 `legacy.get_gateway()`；② `tui` 命令实际执行 `node tui/cli.js --host --port`，依赖系统 node；③ `mcp-server` 支持 stdio / sse / streamable-http 三种 transport |
| 备注 | 与主入口 `dfecrab`（Click）**完全独立、命令不互通**；`--version` 输出 `DFEcrab v3.0.0 - 重构版`，版本号最旧 |

### 1.3 `src/agentscope_compat.py`

| 项 | 内容 |
|---|---|
| 类型 | 兼容壳 |
| 规模 | 20 行 / 1 枚举 + 1 dataclass |
| 职责 | 为依赖 AgentScope 响应对象的旧 skill 提供最小替代实现 |
| 核心符号 | `ServiceExecStatus`（枚举：`SUCCESS` / `ERROR`）、`ServiceResponse`（dataclass：`status` / `content` / `message`） |
| 被调用方 | 旧 skill（如 `self-improving-agent` 系列） |
| 依赖 | 仅标准库 |
| 备注 | 项目实际已不依赖 AgentScope（`requirements.txt` 无此包），此文件是迁移期的桥接层 |

---

## 2. `src/core/`

| 项 | 内容 |
|---|---|
| **定位** | 内核层公共定义：事件协议与错误处理 |
| **分层** | L6 |
| **规模** | 3 个 .py，共 529 行 |
| **上游（谁用它）** | `src/gateway/grpc_server.py`、`src/gateway/event_bus.py`、`src/agent/loop.py`、`src/skill/`、`src/reflection/events.py`、`tests/core/test_kernel.py` |
| **下游（它用谁）** | 仅标准库，**零内部依赖**（这是它有意为之的设计） |

### 2.1 `core/__init__.py`

| 项 | 内容 |
|---|---|
| 类型 | 常量枚举（空） |
| 规模 | 7 行，仅 docstring |
| 职责 | 包声明 |
| 备注 | docstring 明确写着「保留模块：event_types；**其余兼容壳已清理（v5 阶段 E）：无引用方**」——说明此目录曾有过更多文件，已被清理 |

### 2.2 `core/errors.py`

| 项 | 内容 |
|---|---|
| 类型 | 核心逻辑 · 错误处理 |
| 规模 | 218 行 / 2 个 dataclass 异常 + 5 个函数 |
| 职责 | 统一错误处理体系：区分用户错误与系统错误，标准化响应格式 |
| 核心符号 | `UserError`（dataclass 异常）、`SystemError`、`error_response()`、`setup_global_exception_handlers()`、`setup_asyncio_exception_handler()`、`raise_not_found()`、`raise_ambiguous()`、`raise_validation_error()` |
| 被调用方 | `src/gateway/grpc_server.py:52`（导入 `error_response`、`setup_global_exception_handlers`、`setup_asyncio_exception_handler`、`UserError`），在 `grpc_server.py:1094` 完成注册 |
| 依赖 | 仅标准库 |
| 关键行为 | ① `UserError` 返回 `status_code=400`（或 404/自定义），**带 candidates 候选建议 + build_hint 修复提示**，不暴露堆栈；② `SystemError` 与未知异常统一返回「系统内部错误，请稍后重试」+ `status_code=500`，完整堆栈只进日志；③ 设计参考 LibreChat 的 `userError + errorToolResult` 模式（docstring 明示） |
| 备注 | 三个 `raise_xxx` 便捷函数目前**在 `src/` 内未检索到调用方**，可能是给业务代码预留的规范，实际各 Handler 仍各写各的错误返回 |

### 2.3 `core/event_types.py`

| 项 | 内容 |
|---|---|
| 类型 | 常量枚举 · 事件协议 |
| 规模 | 304 行 / 1 枚举 + 16 dataclass + 3 函数 |
| 职责 | **ReAct 事件协议的唯一权威定义**：事件类型枚举、各事件字段规范、构造函数、旧名兼容映射 |
| 核心符号 | `ReactEventType`（枚举，17 个成员）、`LEGACY_EVENT_NAME_MAP`、`make_event()`、`normalize_legacy_event()`、`make_event_envelope()`；16 个事件 dataclass（`ThinkStartEvent` / `ThinkEvent` / `ToolCallEvent` / `ToolResultEvent` / `PlanCreatedEvent` / `PlanStepEvent` / `MessageEndEvent` / `ErrorEvent` / `ConfirmationEvent` 等） |
| 被调用方 | `src/agent/loop.py`、`src/gateway/event_bus.py`、`src/gateway/http/server.py`、`src/gateway/grpc_server.py`、`src/skill/registry.py`、`src/skill/alert_react_pipeline.py`、`src/reflection/events.py`、`services/*_grpc.py` |
| 依赖 | 仅标准库 + 函数内延迟导入 `uuid` / `datetime` |
| 关键行为 | ① **v4.0 起 `THINKING` 拆为三段式**：`think_start` → `think`（多次增量）→ `think_end`；② **旧 `final` 事件已被 `message_end` 取代**，是唯一终止标记；③ `LEGACY_EVENT_NAME_MAP` 提供旧名→新名映射（`reasoning`/`thinking`→`think`、`clarification_needed`→`confirmation`、`final`→`message_end`）；④ `make_event_envelope()` 输出统一信封 `{type:"event", id, data:{event_type, data, session_id, correlation_id}, timestamp}` |
| 关联文件 | 前端对接文档的事件流约定；`src/gateway/handlers/events_handler.py` |
| 备注 | **协议已演进到 v4.0（message_end），但 `README.md` 与 `CODEMAP` 的对话链路图仍写事件类型为 `thinking` / `final`——文档滞后于实现**。另：本文件定义了 16 个 dataclass，但 `make_event()` **并未使用它们做字段校验**，只是 `dict(fields)` 透传——dataclass 实际只起文档作用 |

---

## 3. `src/config/`

| 项 | 内容 |
|---|---|
| **定位** | 配置加载层 |
| **分层** | L6 |
| **规模** | 4 个 .py，共 441 行 |
| **⚠️ 最易踩的坑** | **这里有两个同名的 `Config` 类和两个同名的 `config` 单例**，分属不同文件、语义完全不同，见下方对照表 |

### 3.0 两个 `Config` 的对照（务必看清）

| | `config/config.py` | `config/config_loader.py` |
|---|---|---|
| 类 | `Config` | `Config` |
| 单例 | `config` | `config` |
| 数据来源 | **硬编码在 Python 源码里** | 读 `config/gateway.yaml` |
| 内容 | 模型列表（`ollama_qwen` / `ollama_llama` / `zhipu_glm4_flash`）、系统提示词、**明文 API Key** | gateway 端口、ZK 地址、嵌入配置、服务地址 |
| 属性示例 | `.models`、`.default_model`、`.get_model(id)`、`.SYS_PROMPT` | `.config_data`、`.gateway_port`、`.zk_hosts` |
| 实际使用方 | **仅 `src/gateway/legacy.py`** | `dfecrab`、`src/gateway/grpc_server.py`、`src/gateway/grpc/zk_registry.py`、`src/agent/llm/think.py`、`services/*_grpc.py`、`scripts/check_system_health.py`、`scripts/start_gateway_grpc.py`、`src/__main__.py` |
| 结论 | 只服务**旧版 plugin 模式 Gateway**，生产 grpc 模式不经过它 | **生产路径真正使用的配置源** |

### 3.1 `config/__init__.py`

| 项 | 内容 |
|---|---|
| 类型 | 配置 |
| 规模 | 15 行 |
| 职责 | 导出 `config.py` 的 `Config` / `config` / `Model` / `Message` / `Completion` |
| 备注 | **只导出 `config.py`，不导出 `config_loader.py` 与 `port_loader.py`**。因此 `from src.config import config` 拿到的是**硬编码模型配置**那个，而不是读 yaml 的那个——这是最容易出错的地方 |

### 3.2 `config/config.py`　`[历史遗留]`

| 项 | 内容 |
|---|---|
| 类型 | 配置 · 模型配置 |
| 规模 | 234 行 / 6 类 |
| 职责 | 定义可用 LLM 模型清单、默认模型、系统提示词 |
| 核心符号 | `Model`、`Message`、`Function`、`Tool`、`Completion`、`Config`（含 `config` 单例） |
| 被调用方 | **仅 `src/gateway/legacy.py:10`** |
| 依赖 | 标准库 + 函数内 `from openai import OpenAI` |
| 关键行为 | 三个模型硬编码：① `ollama_qwen`（`deepseek-v3.1:671b-cloud`，`http://localhost:11434/v1`）；② `ollama_llama`（`llama3.1:8b`）；③ `zhipu_glm4_flash`（`glm-4-flash`，`https://open.bigmodel.cn/api/paas/v4/`） |

> ### 🔴 安全问题：明文 API Key
>
> **`src/config/config.py:72` 硬编码了智谱 GLM 的 API Key：**
> ```
> api_key="9c7a0274c4440453e27c908da4f52636.cGEY3A3zA2XXZTYX"
> ```
>
> | 评估 | 结论 |
> |---|---|
> | 是否泄露到公网 | 需按实际仓库可见性判断（若为公开仓库则**已泄露**） |
> | 影响范围 | 该 Key 仅被 `legacy.py`（旧版 plugin 模式 Gateway）使用，生产 grpc 模式不经过它 |
> | **建议动作** | ① **立即在智谱控制台轮换该 Key**；② 改为从环境变量读取（`os.getenv("ZHIPU_API_KEY")`）；③ 若仓库曾公开，需检查是否有异常调用记录 |
> | 处理阶段 | 阶段 8《冗余重复专项报告》将单列「安全项」小节 |

| 备注 | `SYS_PROMPT` 里提到的工具是 `send_message`、`list_agents`、`wait_for_event` 等，与当前 gRPC 多智能体架构描述不符 → **提示词也是遗留版本** |

### 3.3 `config/config_loader.py`

| 项 | 内容 |
|---|---|
| 类型 | 配置 · 加载器 |
| 规模 | 99 行 / 1 类 |
| 职责 | 加载 `config/gateway.yaml` 并暴露为属性 |
| 核心符号 | `Config`、`config`（模块级单例） |
| 被调用方 | `dfecrab`（`from config.config_loader import config`）、`src/gateway/grpc_server.py:204`、`src/gateway/grpc/zk_registry.py:32/201`、`src/agent/llm/think.py:47`、`services/*_grpc.py`、`scripts/check_system_health.py:168/201/625/811`、`scripts/start_gateway_grpc.py:73`、`src/__main__.py:87` |
| 依赖 | `yaml`、`Path` |
| 关键行为 | ① 懒加载，失败时 `self.config_data = {}` 不抛异常；② 属性访问带默认值兜底：`gateway_port`（默认 6789）、`websocket_port`（默认 6790）、`zk_hosts`（默认 `localhost:2181`）、`embedding` |
| 关联文件 | `config/gateway.yaml` |
| 备注 | **导入路径有两套写法**：`from src.config.config_loader import config` 与 `from config.config_loader import config` 都在用（后者依赖 `dfecrab` 把 `src/` 加进 `sys.path`）。混用会让静态分析工具失效，建议统一 |

### 3.4 `config/port_loader.py`

| 项 | 内容 |
|---|---|
| 类型 | 配置 · 端口管理 |
| 规模 | 93 行 / 0 类 / 3 函数 |
| 职责 | **本地端口的单一事实源**，从 `config/gateway.yaml` 的 `local_ports` 读取 |
| 核心符号 | `load_local_ports()`、`knowledge_api_port(default=6788)`、`mcp_port(svc_name, default=None)` |
| 被调用方 | `mcp_servers/mcp_demo.py:34`、`mcp_servers/mcp_alert_judge.py:200`、`mcp_servers/blackxml_topology.py:44`、`src/gateway/grpc_server.py:2068`、`scripts/start_knowledge_api.py:48`、`scripts/knowledge_api.py:249/271` |
| 依赖 | `yaml`、`Path` |
| 关键行为 | ① 两级路径查找：`src` 包相邻目录 → cwd 相邻目录，兼容 `python -m` 与直接运行两种场景；② 失败时记录 warning 并返回空 dict，不抛异常 |
| 关联文件 | `config/gateway.yaml` 的 `local_ports` 节点；`config/mcporter.json`（**端口需与之保持一致，`dfecrab` 启动时会比对并告警**） |
| 备注 | 模块 docstring 第 8 行的示例 `from config.port_loader import ...` 与标准写法 `from src.config.port_loader import ...` 不一致；示例注释 `mcp_port("demo") # 8600` 与实际默认端口结构（`8600` / `8601` / `8602`）描述模糊 |

---

## 4. `src/utils/`

| 项 | 内容 |
|---|---|
| **定位** | 通用工具集合 |
| **分层** | L6 |
| **规模** | 7 个 .py，共 744 行 |
| **上游（谁用它）** | `src/gateway/websocket/`、`src/gateway/session_handler.py`、`src/memory/modules/`、各服务启动脚本、各日志模块、PDF 处理链路 |
| **说明** | 不是单一职责的目录，实际混杂了 **日志工具（4 个）** 与 **业务工具（3 个：mermaid 校验、中文时间解析、docx 读取）** 两类 |

### 4.1 `utils/__init__.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 |
| 规模 | 11 行 |
| 职责 | 导出 `DailyTrimmedFileHandler` 与 `parse_time_reference` |
| 备注 | **只导出 2 个符号**，其余 5 个模块（`logger`、`logging_setup`、`mermaid_validator`、`time_parser` 其余函数、`word_reader`）需从子模块直接导入 |

### 4.2 `utils/logger.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 日志 |
| 规模 | 7 行 / 1 函数 |
| 职责 | 获取命名空间下的 logger |
| 核心符号 | `get_logger(name)` → `logging.getLogger(f"dfecrab.{name}")` |
| 被调用方 | `src/memory/modules/compactor.py:36`、`src/memory/modules/consolidator.py:123`、`src/memory/modules/reflector.py:57`、`src/gateway/websocket/connection_manager.py:118`、`src/gateway/session_handler.py` |

### 4.3 `utils/log_handler.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 日志 |
| 规模 | 70 行 / 1 类 |
| 职责 | 单文件滚动 + 按天裁剪的日志 Handler，避免累积大量历史日志文件 |
| 核心符号 | `DailyTrimmedFileHandler` |
| 被调用方 | 通过 `src/utils/__init__.py` 导出，被 `logging_setup.py` 使用 |
| 依赖 | `logging.handlers.RotatingFileHandler` |
| 关键行为 | ① 单文件滚动（`maxBytes` / `backupCount`），不按天分文件；② 启动时检查文件 mtime，跨天则**清空重写**；③ 构造函数会 `os.makedirs` 建目录，并把 delay / encoding 设为 `True` / `utf-8` |
| 备注 | 与 `logging_setup.setup_root_logging` 的 `RotatingFileHandler` 形成「统一日志初始化」链条 |

### 4.4 `utils/logging_setup.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 日志 |
| 规模 | 146 行 / 3 函数 |
| 职责 | 各服务的统一日志初始化与启动横幅写入 |
| 核心符号 | `setup_root_logging(log_file, service_name="dfecrab", level=INFO)`、`setup_service_logging(service_logger_name, log_file, level=INFO)`、`write_startup_banner(log_file, service_name)` |
| 被调用方 | `scripts/start_gateway_grpc.py:21`（安装 3 条链路）、`services/manager_agent/manager_agent_grpc.py:39`、`scripts/start_knowledge_api.py:52` |
| 关键行为 | ① **三条日志链路**：根 logger→`gateway_grpc.log`、`dfecrab.manager`→`manager_agent.log`、`dfecrab.react`→`worker_agents.log`；② `write_startup_banner` 用 `w+` 截断写入，确保每个日志文件首条是启动横幅；③ 统一格式 `%(asctime)s %(levelname)s %(name)s | %(message)s` |
| 关联文件 | `logs/` 目录；`scripts/start_gateway_grpc.py` |
| 备注 | 与 `utils/log_handler.py` 的 `DailyTrimmedFileHandler` 存在**功能重叠**（都在做"只保留当天日志"），建议评估是否可合并 |

### 4.5 `utils/mermaid_validator.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 校验 |
| 规模 | 260 行 / 1 类 + 1 函数 |
| 职责 | 校验并自动修复 Mermaid 流程图代码块的语法 |
| 核心符号 | `MermaidValidator`、`ensure_mermaid_format()`（模块级便捷函数） |
| 被调用方 | `src/gateway/handlers/`、`services/manager_agent/manager_agent_grpc.py`、`scripts/check_system_health.py` |
| 依赖 | 仅标准库（`re`） |
| 关键行为 | ① 8 类校验：未闭合中括号 / 花括号 / 圆括号、未匹配双引号、丢失分号、孤立箭头、CSS `style`/`class` 语法；② **自动修复**：补全缺失的分号与引号；③ `fail_fast` 参数控制是否在首个错误处停止 |
| 备注 | 用于网关响应中的 Markdown 图表校验（防止前端渲染失败）。属于**面向 LLM 输出的防御性工具** |

### 4.6 `utils/time_parser.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 时间解析 |
| 规模 | 214 行 / 0 类 / 6 函数 |
| 职责 | 把中文自然语言时间描述解析为具体日期区间 |
| 核心符号 | `get_time_context()`、`parse_time_reference()`、`_parse_relative_days()`、`_parse_relative_months()`、`_parse_weekday()`、`_parse_range()` |
| 被调用方 | 通过 `src/utils/__init__.py` 导出 `parse_time_reference`；`src/gateway/handlers/`、`skills/` 的业务逻辑 |
| 依赖 | 标准库 `datetime` / `calendar` / `re` |
| 关键行为 | 支持：相对天数（"最近 7 天" / "过去 3 天"）、相对月份（"最近 3 个月" / "上个月"）、星期（"本周一" / "上个周三"，用 `calendar` 处理跨周）、显式日期范围（"3月1日到3月31日"）；默认回退返回当前日期 |
| 备注 | 这是**面向电网业务查询**（如负荷分析、告警查询）的关键工具，中文时间表达的解析质量直接影响业务准确性 |

### 4.7 `utils/word_reader.py`

| 项 | 内容 |
|---|---|
| 类型 | 工具类 · 文档解析 |
| 规模 | 36 行 / 1 函数 |
| 职责 | 读取 `.docx` 文件的纯文本内容 |
| 核心符号 | `read_word(file_path, extract_tables=True)` → `str` |
| 被调用方 | `skills/word_read/`、`src/knowledge/` 的文档导入链路 |
| 依赖 | `docx`（python-docx），缺失时抛出带安装提示的 `ImportError` |
| 关键行为 | ① 提取所有段落文本；② 默认同时提取表格（`extract_tables=True`），表格按行拼接、单元格用空格分隔；③ 文件读取失败时抛出 `ValueError` |
| 关联文件 | `skills/word_read/`、`knowledge_base/documents/`（有 3 个 .docx） |

---

## 5. 本组发现汇总

| # | 级别 | 发现 | 位置 |
|---|---|---|---|
| 17 | 🔴 **安全** | **明文 API Key 硬编码**（智谱 GLM） | `src/config/config.py:72` |
| 18 | 🟠 P1 | **两个同名 `Config` 类 + 两个同名 `config` 单例**，`__init__.py` 只导出硬编码那个，极易误用 | `src/config/config.py` vs `src/config/config_loader.py` |
| 19 | 🟠 P1 | 硬编码模型配置的 `config` **仅有 `legacy.py` 一个使用方**，属遗留代码 | `src/gateway/legacy.py:10` |
| 20 | 🟡 P2 | **四个版本号互不一致**：`src/__init__.py`=4.0.0、`dfecrab`=4.0.0-grpc、`__main__.py`=v3.0.0、`README.md`=v4.1 | 全局 |
| 21 | 🟡 P2 | `config_loader` 存在 `src.config.*` 与 `config.*` **两套导入写法**混用 | 多处 |
| 22 | 🟡 P2 | 事件协议已演进到 v4.0（`message_end` 取代 `final`、`thinking` 三段式），但 `README.md` 与 CODEMAP 链路图仍写 `thinking` / `final` | `src/core/event_types.py` vs 文档 |
| 23 | 🟡 P2 | `event_types.py` 定义了 16 个事件 dataclass，但 `make_event()` **不做字段校验**，dataclass 实际只起文档作用 | `src/core/event_types.py` |
| 24 | 🟡 P2 | `core/errors.py` 的 `raise_not_found` / `raise_ambiguous` / `raise_validation_error` 三个便捷函数**在 `src/` 内无调用方** | `src/core/errors.py` |
| 25 | 🟡 P2 | `get_plugins_path()` 已无实际调用方（`plugins/` 未被加载） | `src/__init__.py` |
| 26 | 🟡 P2 | `logging_setup` 的 `RotatingFileHandler` 与 `DailyTrimmedFileHandler` **功能重叠** | `src/utils/` |

---

<div align="center">

**02.0 · src 根与 core.config.utils 完**　（17 / 17 个文件）

上一页：[01-顶层目录总览.md](../01-顶层目录总览.md)　｜　下一页：[02.1-agent与LLM.md](./02.1-agent与LLM.md)

</div>