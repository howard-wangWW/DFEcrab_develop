# DFEcrab 依赖清单
# 生成依据: 线上运行环境 /home/e8900/DFEcrab/venv/ (Python 3.12.7)，pip freeze 于 2026-08-25
# 安装: ./venv/bin/python3 -m pip install -r requirements.txt
# 离线安装(内网/断网): ./venv/bin/python3 -m pip install -r requirements.txt --find-links wheels/ --no-index
#   注意: wheels/ 仅含核心运行时包(cp312 manylinux)，不含 torch/AI 栈；
#         完整离线安装需为 torch/transformers/nvidia-* 等单独准备离线源。
#
# 说明:
#  - 以下为环境全量依赖，已排除 pip/setuptools/wheel 等构建工具。
#  - 旧 requirements.txt 漏列了 AI/LLM(grpc/pydantic/mcp) 等核心依赖，本次已补全；
#    并移除了环境中实际未安装的 prompt_toolkit / textual / flask-cors / bcrypt / rank_bm25。
#  - 异常包 httpx2 / httpcore2 疑似重复安装，已剔除，建议排查 `pip show httpx2`。

# ===== Web / API 网关 (Gateway gRPC) =====
fastapi==0.135.3
uvicorn==0.44.0
starlette==1.6.0
sse-starlette==3.4.8
Flask==3.1.3
Werkzeug==3.1.8
itsdangerous==2.2.0
blinker==1.9.0
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.3.2
python-multipart==0.0.32
email-validator==2.3.0
websockets==17.0.1

# ===== HTTP / 异步客户端 =====
requests==2.33.1
httpx==0.28.1
httpx-sse==0.4.3
aiohttp==3.13.5
aiohttp-cors==0.8.1
aiohappyeyeballs==2.6.1
aiosignal==1.4.0
frozenlist==1.8.0
multidict==6.7.1
yarl==1.23.0
propcache==0.4.1
attrs==26.1.0
anyio==4.13.0
h11==0.16.0
sniffio==1.3.1
httpcore==1.0.9

# ===== 配置 / 数据校验 =====
PyYAML==6.0.3
python-dotenv==1.2.2
pydantic==2.12.5
pydantic_core==2.41.5
pydantic-settings==2.15.0
typing_extensions==4.15.0
typing-inspection==0.4.2
annotated-types==0.7.0
annotated-doc==0.0.4
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==2026.6.3
packaging==26.3
jsonref==1.1.0
pathable==0.6.0
jsonschema-path==0.5.0
openapi-pydantic==0.5.1

# ===== AI / LLM / 向量嵌入 (torch GPU 栈) =====
openai==3.0.0
transformers>=5.0.0,<6.0.0      # 对齐 sentence-transformers 6.0.0 (>=5.0.0) 解决 pip check 冲突；升级前请确认与 torch==2.4.1 兼容
torch==2.4.1
sentence-transformers==6.0.0
huggingface-hub>=1.3.0,<2.0.0   # 对齐 sentence-transformers 6.0.0 (>=1.3.0,<2.0.0)
tokenizers==0.20.3
safetensors==0.8.0
scikit-learn==1.5.2
scipy==1.14.1
numpy==1.26.4
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
joblib==1.5.3
threadpoolctl==3.6.0
regex==2026.7.19
triton==3.0.0
# torch 的 CUDA 依赖（仅 GPU / Linux x86_64 需要；CPU 部署可移到可选 extra）
nvidia-cublas-cu12==12.1.3.1
nvidia-cuda-cupti-cu12==12.1.105
nvidia-cuda-nvrtc-cu12==12.1.105
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cudnn-cu12==9.1.0.70
nvidia-cufft-cu12==11.0.2.54
nvidia-curand-cu12==10.3.2.106
nvidia-cusolver-cu12==11.4.5.107
nvidia-cusparse-cu12==12.1.0.106
nvidia-nccl-cu12==2.20.5
nvidia-nvjitlink-cu12==12.9.86
nvidia-nvtx-cu12==12.1.105

# ===== 知识库向量检索 =====
faiss-cpu==1.8.0
jieba==0.42.1

# ===== 文档解析（知识库上传 PDF/DOCX/XLSX 依赖） =====
pymupdf==1.28.2      # PDF 解析；document_loader 依赖，缺失会导致 PDF 上传报"请安装 pymupdf 或 PyPDF2"
# Excel 解析：.xlsx/.csv 走纯标准库（zipfile + csv），无需任何第三方依赖；
#             .xls（老 BIFF 二进制）需 xlrd（纯 Python，wheels/ 中已备）
xlrd==2.0.1          # .xls 解析；缺失时 .xls 上传报明确安装提示，不影响 .xlsx/.csv
# python-docx==1.2.0  与 lxml==6.1.2 见下方"工具/杂项"（均已安装，wheels/ 中亦均有对应 wheel）

# ===== 知识库 OCR（图片文字识别）=====
# 不在此文件内声明！见 requirements-ocr.txt。
# 原因：rapidocr-onnxruntime 声明依赖 opencv-python（完整版），而本方案用
# opencv-python-headless 替代以免依赖系统 libGL.so.1，包名对不上会使本文件的
# 「离线一键安装」整体失败。故 OCR 依赖独立成文件，用 --no-deps 二次安装。
#   ./venv/bin/python3 -m pip install -r requirements-ocr.txt --no-index --find-links wheels/ --no-deps

# ===== gRPC / 服务发现 (Zookeeper) =====
grpcio==1.80.0
protobuf==7.34.1
kazoo==2.11.0

# ===== MCP 服务 =====
mcp==1.29.0
fastmcp==3.4.7
fastmcp-slim==3.4.7
cyclopts==4.23.0
docstring_parser==0.18.0

# ===== 终端 UI / 日志 =====
rich==15.0.0
rich-rst==2.1.0
Pygments==2.21.0
markdown-it-py==4.2.0
mdurl==0.1.2

# ===== 认证 / 加密 =====
PyJWT==2.13.0
cryptography==50.0.0
cffi==2.1.1
pycparser==3.0
Authlib==1.7.2
joserfc==1.7.4
cachetools==7.1.7

# ===== 工具 / 杂项 =====
psutil==7.2.2
certifi==2026.2.25
charset-normalizer==3.4.7
idna==3.18
urllib3==2.6.3
distro==1.9.0
fsspec==2026.7.0
filelock==3.32.3
aiofile==3.12.3
caio==0.12.2
beartype==0.22.9
watchfiles==1.2.0
lxml==6.1.2
dnspython==2.8.0
exceptiongroup==1.3.1
griffelib==2.2.0
keyring==25.7.0
jaraco.classes==3.4.0
jaraco.context==6.1.2
jaraco.functools==4.6.0
jeepney==0.9.0
SecretStorage==3.5.0
platformdirs==4.11.3
more-itertools==11.1.0
py-key-value-aio==0.4.5
pyperclip==1.11.0
truststore==0.10.4
uncalled-for==0.4.0
opentelemetry-api==1.44.0
python-docx==1.2.0
tqdm==4.70.0
