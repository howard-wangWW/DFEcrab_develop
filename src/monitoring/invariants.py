"""
运行时不变量检查。

当前仓库中原始实现已不在代码树内，这里只暴露一个明确的占位结果，
避免路由继续导入不存在的旧模块。
"""


def check_all_invariants():
    return {
        "status": "unavailable",
        "reason": "invariants module is not implemented in the migrated src layout",
    }
